spring:
  application:
    name: portal-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/dev/
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  profiles:
    active: native


